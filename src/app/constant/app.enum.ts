export enum APP_ENV {
  PROD = 'production',
  STAGING = 'staging',
  DEV = 'development',
}

export enum ELocale {
  vi = 'vi',
  en = 'en',
}

export enum EStatus {
  inactive,
  active,
}

export enum EPostCategoryType {
  list = 1,
  tab,
}

export enum ESystemType {
  text = 1,
  number,
  textarea,
  password,
  editor,
  json,
}

export enum EType {
  project = 1,
  partner,
}

export enum EPropertyLevel {
  parent = 1,
  child,
}

export enum EMemberType {
  managers = 1,
  executive_board,
}

export enum ECapacityProfileType {
  manager = 1,
  board_of_director,
  editor,
  hyperlink,
}

export enum ELocationType {
  city = 1,
  district,
}

export enum EGenderContact {
  mr = 'mr',
  ms = 'ms',
  mrs = 'mrs',
}
